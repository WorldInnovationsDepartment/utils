# upscale-video

Upscale video using Real-ESRGAN via Replicate API

## Prerequisites
replicate, requests, tqdm, python-dotenv, FFmpeg, REPLICATE_API_TOKEN

## Script
upscale_video.py

## Usage
```bash
cd ~/BRAIN/utils && python cli.py upscale-video --help
```

## Source
Original: ~/BRAIN/utils/upscale_video.py
